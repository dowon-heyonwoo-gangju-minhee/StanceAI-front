import LoginForm from "@/components/LoginForm";

export const metadata = {
  title: "login",
};

const LoginPage = () => {
  return (
    <div>
      LoginPage페이지입니다.
      <LoginForm />
    </div>
  );
};

export default LoginPage;
